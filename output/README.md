# Generated transcription outputs

Generated files:

- `lw044_terms_transcription.json` — structured UTF-8 JSON transcription preserving paragraphs, clauses, definitions and tables.
- `lw044_terms_transcription.html` — standalone semantic HTML transcription with readable CSS and actual HTML tables.

Source priority:

- Authoritative source: `LW044_AD_D_TTD_PTD_PL_3.pdf`.
- Helper transcription source: `LW044_AD_D_TTD_PTD_PL_3.docx`.
- The DOCX was used to accelerate text extraction; the PDF and `structure_spec.md` were treated as the structural and visual reference for page count, sections, paragraph symbols and table handling.

Uncertain OCR/transcription points:

- No fragments were marked as uncertain in the generated JSON/HTML.
- One obvious Polish OCR artifact from the DOCX helper (`nie sią należne`) was corrected to `nie są należne` in the definition of `Okres wyczekiwania` and recorded in JSON quality-control metadata.
